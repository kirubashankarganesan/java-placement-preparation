Recursion is a process in which a method calls itself again and again until a condition is satisfied.

Basic concept

Every recursive program has 2 important parts:

   1. Base condition → stops the recursion.
   2. Recursive call → method calls itself.

